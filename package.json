{
  "name": "deetsmusic",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "dev:app": "node scripts/dev-app.mjs",
    "cli:build": "cargo build --release --manifest-path cli/Cargo.toml && node scripts/cli-dist.mjs",
    "release": "npm run cli:build && tauri build && node scripts/release-check.mjs && node scripts/archive-installer.mjs",
    "release:check": "node scripts/release-check.mjs",
    "signin:assets": "node scripts/signin-assets.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.0.3",
    "typescript": "~5.6.2"
  }
}
