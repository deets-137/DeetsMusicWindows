# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Dev-only raw API dumps (real account data)
dev-dumps/

# Local Claude Code settings
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.obsidian/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated dev + beta configs (scripts/dev-app.mjs, scripts/release.mjs --beta) + the CLI
# crate's build dirs (the beta CLI builds apart, so neither overwrites the other)
src-tauri/.tauri.dev.gen.json
src-tauri/.tauri.beta.gen.json
cli/target/
cli/target-beta/

# Local archive of shipped NSIS installers (scripts/archive-installer.mjs), kept the
# way DeetsAirplay keeps its installers/ — handy to re-install an older build, too big
# to carry in git (~6 MB each).
installers/

# heaviness sampler output (scripts/heaviness-sample.ps1 -Loop)
scripts/heaviness-samples.log

# The owner's taste profile (what Claude RECOMMENDS; he decides). Kept on this PC.
# docs/LESSONS.md is tracked since 2026-09-19.
docs/TASTE.md

# The web demo build (docs/features/WEB-DEMO.md)
dist-web

# Raw notes for the new-app template (local only until synthesized)
docs/ops/deetsapp-template.md

# A local [patch] for the AirPlay crate while a crate change is under test (src-tauri/Cargo.toml)
src-tauri/.cargo/
